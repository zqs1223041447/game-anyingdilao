# 工作过程日志 (Worklog)

> 每次重要事件追加时间线条目，新条目在顶部。

## 2026-09-02

- **V1.35 DLL 版本回退修复 + 防旧产物构建链交付**：确认 `GenerateAssemblyInfo=False` + `AssemblyInfo.cs` 固定 `0.0.0.0` 导致新 MOD 代码已编入但文件版本元数据回退；新增 `Directory.Build.props` 集中版本源（FileVersion 1.35.0.0 / ProductVersion 1.35.0，Unity 程序集身份 0.0.0.0 保持兼容），启用 SDK AssemblyInfo 生成并清空旧属性文件。新增 `build-mod.ps1/.cmd`（引用预检、clean、no-incremental、GUID 独立输出、生成时间/版本/SHA 三重校验、同版本拒绝覆盖、升级包生成）、哈希与版本双校验安装器模板、`verify-dll.ps1`、中文完整指南。纯 GitHub 仓库缺 gitignored 的 127 refs/游戏 DLL，故不伪报构建成功；完整编译、DLL SHA 与游戏冒烟留用户含本体的 Windows 工作区执行。

## 2026-09-01

- **V1.34 完整版重做（新原版唯一基线逐行）**：按用户提议以 `fresh_new` 为唯一基线逐行重做，备份 `69C0D965` 至 `backup/pre-rebuild-*`，回拷 895 .cs 基线，疏理 43 文件 MOD 清单（895 全部一致前每步校验），品质背景图 `QualityColor.SlotColors` 完整保留，`dotnet build 0 error`/`895 全部一致`，新构建 `82AF138C…599` 与 `Game-Later 92E0120F` typetree 对齐。

## 2026-09-01

- **Game-Later 新基线重建 + V31 重新生成（全量 typetree 修复，人工测试）**：完整版游戏目录更新至 `Game-Later/`（vanilla `92E0120F…2D52`，127 Managed）；`MODworkv2/refs` 全量同步 127 DLL；ILSpy 8.2 重新反编译 923 .cs（修复 `array[^1]`×2 + `RefSafetyRules(11)` 后 vanilla 0 error）；以 2026-08-30 版 V1.31 构建 `2216612A…` 反编译树回灌全部 MOD 代码到新 vanilla；初版重建 `358ACF51…`/`CDEF29C2…` 因序列化错位已作废（`WeaponClass.Enchanted` / `WeaponSaveData.Enchanted` 及 `Hand/ItemScript/ContainerItemData/SK_FlySowrd/SettingBT/PlayerManager` 6 项新字段）；最终 `69C0D965…83528BB`（2,451,968 字节，0 error/121 warnings，全量 typetree 对齐，`different serialization layout` + `level1 is corrupted` 已修复；`ZhaDao/SK_FlySowrd` 32/92→160/96 已随 Hand/SK_FlySowrd 等修复而消除）；新升级包 `ShadowDungeon-MOD-V1.31_2026-09-01.zip`（`D321D479…867E2`，`8AE99F38…`，HASH_MATCH_PASS）。leopard-x 诊断：ZhaDao 128 字节为依赖闭包截断、SK_FlySowrd 4 字节为单字段缺失、PlayerManager 20 字节为多字段结构差异——三类错误分治修复。

## 2026-08-30

- **V1.32 二/三/四轮修订：遥测驱动定位并移除星环 BuffTime 死门（真机截图闭环三轮定位）**：①二轮——用户截图实证两件装备悬停时均在背包（诊断行 槽6/槽7=原生装备），诊断行改 `⚠ 未穿戴…右键装备后生效`/`✓ 已穿戴生效`；②三轮——用户截图实证已穿戴（✓ ring=True chain=True）但仍无环：`SpawnExtraProjectiles` 各早退分支接入**出手遥测**（LastCastInfo 悬停可见）+ **TargetPos 环向修正**（克隆弹沿用本体目标点会收拢成一点）；③四轮——遥测回传 `[FireBall] 被 BuffTime=1 门控拦截` 实锤终极根因：V1.23 遗留 `dt.BuffTime>0` 死门（SK_Fly 四族与 CreatSP 均不消费该字段），**移除**；克隆弹经 gun.CreatSP 按当前技能样例全量构建（无保真度问题）。每轮构建 0 error → 部署 SHA256 一致 → 42s 冒烟 LOG CLEAN；SHA 链 53DF7800(一轮)→891FD474(二轮)→3D6BCA8B(三轮)→**168433A6(四轮现行)**，zip 现行 `39AB1801…B92F`。**事故与纠正**：二轮包 install.ps1 expectedHash 手抄错一位且自检靠肉眼没发现——三轮起改为脚本化一致性断言（HASH_MATCH_PASS）。**方法论沉淀**：tooltip 遥测行=无需日志回传的截图闭环调试；同功能修复先查树内已有通道（撞号会话上午已修 case 0/2 总出口，我方 ACTbar 通道取证后撤销避免双通道重复 +4）。**待真机**：火球 5 弹成环（对照天钻护符「元素引集新星」原生环形观感）。

- **V1.32 词条档位双回退 + 识别诊断行出包部署 PASS（承接并行会话星环总出口）**：用户截图两反馈：词条标注覆盖不全（固定词条无标注/16% 落在 [21-26] 外）+ 星环回响仍无效果。①AffixTierDisplay 补**全局档位梯**（模板固定词条用整张池表该 Index 档位，T=全游戏名次）与**品质档家族池回退**（同名基底全品质池并集，治点金升品质后词缀属低档池的错位）；②星环根因=挂钩在 case 0/2 内 FStype∉{0,2} 不走星环——并行会话上午已在 V1.31 重建版以总出口修复（2216612A，10:07）；本会话下午试验 ACTbar Count_F 通道后取证撤销（SK_Fly 族不读 Count_F+双通道重复 +4 风险）；③IsEquipped 去 hasWeapon 硬性要求 + tooltip 灰色 `[MOD] 识别:` 诊断行（悬停星环/回响即见装备槽扫描，截图替代日志回传）。撞号取证：Gun.cs 10:02 mtime + V1.31 zip 10:07 重建；本会话 17:35 部署（505DDA87 双通道）复核后撤销重构建（53DF7800 终版）。构建 0 error(125) → 部署 SHA `53DF7800…3F0D` 一致 → 42s 冒烟 LOG CLEAN → V1.32 zip `85819A1C…E798` 解包自检 PASS。CHANGELOG/status/code-index/todo 已登记。**待真机**：星环任意 SK_Fly 技能 +4 环、回响识别行、词条全标注。

- **V1.31 同号合并修订：星环总出口修复 + 与词条档位显示会话合并出包（火球术/冰晶术无环根因修复）**：用户实测火球术/冰晶术无环。根因：星环出手点钩子原挂在攻击函数 case 0/2 内——MGCattack 有 11 个 FStype 分支（0~10），火球/冰晶的 FStype 走进无钩子分支。修复=删 5 处 case 内调用、在 4 个攻击函数 switch 之后统一调 `SpawnExtraProjectiles(this, dt)`（弹体位姿已在 case 内定型；组件守卫+BuffTime 守卫保留，放置型不误生环）。撞号协调：并行会话同期以 V1.31 出“词条档位显示”包（EE20C084，09:56 树，不含本修复）；本会话 10:05 合并构建 `2216612A60C8818F9A937BD33FBED4AC6F59FB85FB51CA3C671E193DF6971EC6`（双方功能齐）已部署+42s 冒烟 PASS，包 README/CHANGELOG 已对齐合并说明。zip `035E3EFA…0700`。
- **V1.31 词条档位显示（T几·第X名/共N档·可roll范围）出包部署 PASS（并行撞号协调）**：用户要求装备词条显示 T 几（T1 满档）+同类第 X 名+可 roll 范围，并先核对原生词条叫啥。调研：原生无 Tier 体系，显示名=Main_FY/MainDisplay_FY/SKStat_* 键；池内同 Index 天然 2~3 条 NB 档位（1030:[4,3,2] 实证）；roll=NB×等级段乘数（0.9~1.6），Level/Quality/DropScene 已持久化可重建上下文。落地：`PoedbMod/AffixTierDisplay.cs`（静态纯显示层：CraftFindTemplate 反查池→同 Index/SkillName|Index 档位降序→可达区间重建（复刻 GenerateWeaponStatValue 系，源行号标注）→值落区间定名次→多档 T=排名/单档百分位 T1~T5/T1+ 超额标）+ `WeaponClass.cs` 四处循环注入（Main/DOT/SK/CP，套装行/空行不碰）。构建 0 error(125) → 部署 SHA `EE20C084…2941` 一致（首轮格式 `[T1｜第2名/共3档｜可roll 9~26]` 出包后，用户定稿 `T1 | [9-26]` 原地修订重出包，SHA/包全链路更新，V1.25 同号修订先例） → 42s 冒烟 LOG CLEAN。升级包 V1.31 zip（`DDBB5E7E…7A4AA`，解包自检 expectedHash==DLL SHA PASS）。**撞号处理**：构建时树内已含并行会话 V1.30 源码（mtime 取证 08:18-08:19 早于我方构建 08:30）→ DLL=三方合并体；曾误将我方 install.ps1/DLL 写入 V1.30 包目录，已从其 zip 原样还原（SHA 复验 `80E42E0E…` 一致）；CHANGELOG/status/todo 已双向登记。设计记录 `docs/research/affix-tier-display-design.md`。**待真机**：悬停装备看词条行尾档位标注；工艺台重骰后档位联动。

- **V1.30 回响 +1 穿透/返程双命中 + 星环补全与诊断出包部署 PASS（真机五回弹）**：用户反馈：①星环 +4 未生效未成环、"似乎和万箭不是同一个效果"（万箭可见→SpawnExtraProjectiles 在跑，星环 flag 或技能路径存疑）②词条颜色已生效③要求回响之链 +1 穿透 + 去程/返程都可命中（对齐 poedb "出去/返回路径各命中一次"）。实现：①`pierceLeft`（回响=1）：SK_FlyA 三处命中点换 `ChainPierceOrReturn()`、SK_FlyBall Stop 门控加穿透分支——**穿透优先于返回**；②三族 StartReturn 不再关碰撞/伤害，OnTriggerEnter 顶部 `if (returning) { ReturnHit(); return; }` 返程只结算 EM_Set 伤害（em 去重，同目标去返各一次）；③SK_Angle_F type 3（原生全环）/4（随机）补 ringMode→SpawnEvenRing(Count+4)；④`IsEquipped` 加 GlobalID 91003/91004 兜底；⑤出手点 `cast:` 日志 + ring=False 装备槽 `equip-dump`（5s 节流）——星环根因下次日志见分晓。构建 0 error(125) → 部署 SHA256 `80E42E0E…E65`（2,446,336 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `ShadowDungeon-MOD-V1.30_2026-08-30.zip`（SHA256 `13403474…CFC4`）。CHANGELOG/status/todo/code-index 已更新。**待真机**：回响穿透/返程命中；星环若仍无效→回传 `[PoeItemMod]` 行。

- **V1.29 星环之戒重做（+4 环状发射）+ 霓虹词条出包部署 PASS（真机四轮回弹 + poedb 专项调研）**：用户指示读 poedb/cn/Projectile 页并检查机制优先级、指出星环"仍然没有修改环形发射机制"、要求特殊词条颜色惊艳。调研（落盘 `docs/research/poedb-projectile-mechanics.md`）：①页面「效果类别」=每击中一个效果按优先级**裂化→穿透→分裂→连锁→返回**（返回最低，"优先程度在穿透/分裂/连锁之后"；出去/返回路径各可命中一次）②星环原型=**破碎传承者 Sire of Shards**"+4 额外投射物+投射物以环状方式发射"——附加量与环状发射是同一条词条两半，V1.28 只均分已有投射物、不镶珠宝单发不成环=用户看到"没效果"的根因。修复：`RingBonusProjectiles=4` 常量；单体弹 SpawnExtraProjectiles 数量层合并（本体+珠宝+4 均分 360°）；弹幕族 SpawnEvenRing(Count+4) 参数化并删 Count>1 门控（单发弹幕也成环；不动 Count 字段避免影响 `timeA>Count*FStime1` 寿命公式）。优先级核对：穿透>返回已满足（SK_FlyBall/Follow 返回门控在 `if(!AllChuan)` 命中停止分支内、穿透分支外）。颜色：`SpecialModColor="#00E5FF"` 霓虹青常量，三处 GetMain 描述行统一引用（替换 V1.28 灰 #9AA0A6）。构建 0 error(125) → 部署 SHA256 `92ACEF22…C1F` 一致 → 42s 冒烟 LOG CLEAN。升级包 `ShadowDungeon-MOD-V1.29_2026-08-30.zip`（SHA256 `D44800AF…6831`）。CHANGELOG V1.29 + status + todo + code-index + 调研已落盘。**待真机**：穿星环任意技能成完整圆环、霓虹青描述行、回响之链回归。

- **V1.28 装备描述 + 星环均分 360° + 回响之链全线生效出包部署 PASS（真机三轮回弹三点反馈）**：用户真机验收 V1.27（商店 5 件已确认齐）后反馈：①两件传奇装备 tooltip 没有描述②星环之戒环形发射=每次发射一个随机方向（黄金角逐发旋转），要求 N 个投射物共享 360°③回响之链返回不生效（法师测试）。定位：①游戏 tooltip 面板（GameUIManager WP_*A 字段组）根本没有 flavor 描述字段，V1.23 注入的 `Item_FY.info_*` 键从未被读取（名字键正常所以只缺描述行）→ 新增 `PoeItemMod.Descriptions`+`TryGetDescription`（按 CurrentLanguage 取中/英），挂进 `WeaponClass.GetMain`/`UseItemClass.GetMain`/`BaoshiClass.GetMain` 词条区灰色首行。②删 Gun.cs 四处黄金角（MGC/SQS/ARC/DEAD attack case0）+`NextGoldenAngle`，`SpawnExtraProjectiles` 重写：RingEquipped 时全部投射物按 `基准角+i*360/(extra+1)` 均分（rotation+dic 双写覆盖 RTtypeOBJ 0/1）；弹幕族 SpawnEvenRing 维持。③返回钩子原仅在 SK_FlyA——`CreatSP` 恒置 `ZY=true`（玩家弹）而敌人弹 false 是回旋门控；移植到 SK_FlyBall/SK_FlyFollow：`[NonSerialized] ReturnToPlayer`（typetree 安全）+SetStart 置位+Stop/TimeStop 返回优先+Update 返回段飞回 yao（0.6 抵达/5s 保护）；SK_FlySowrd 环绕飞剑原生回身边不参与。构建 0 error(125) → 部署 SHA256 `19AF8062…D851`（2,443,264 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `ShadowDungeon-MOD-V1.28_2026-08-30.zip`（SHA256 `15A86005…1D52`，install.ps1 断言式替换核对）。CHANGELOG V1.28 + status + todo + code-index 已更新。**待真机**：描述行显示、2 发 180° 均分、法师弹飞回。

## 2026-08-29

- **POE 词缀映射 + 传奇珠宝适配调研落盘（任务 A/B，纯调研零 DLL 改动）**：用户要求①把 POE 装备词缀按暗影地牢物品分类读取保存 ②把 POE 传奇珠宝效果读取并解析适配性。执行：①探明 POEDB 数据载体——列表页（/cn/Amulets 等 101 页）静态只有隐式词缀+传奇区，**显式词缀池在基底详情页**（/cn/Onyx_Amulet 等静态渲染全池带数值范围）；传奇珠宝清单=/cn/Jewels 的 UniqueItems 链接（97 颗去重）②新管线 5 脚本（`tools/poedb-pipeline/fetch_affixes.py`（101 页全量+缓存续传）、`fetch_affixes_bases.py`（10 槽位类代表基底补抓，多候选轮试）、`fetch_unique_jewels.py`（97 颗详情页）、`analyze_affixes.py`（语义规则引擎映射分级）、`analyze_jewels.py`（97 颗逐颗人工判读分级））③产出：**词缀 6169 条**按暗影地牢槽位归并（主手 1887/护符 1633/珠宝 779/戒指 679/腰带 376/药剂 207/副手 169/头盔 95/胸甲 26/手套 23/鞋子 21），分级 **A 3116/B 556/C 580/D 533/?1360**；**传奇珠宝 97 颗逐颗分级 A 6/B 21/C 19/D 51**——可落地 27 颗集中在同伴强化/投射物/装备共鸣/暴击格挡/权衡词缀/同类珠宝计数六方向，D 51 颗=树半径/三围/球系统/永恒珠宝四大 POE 专有族④落盘 `data/poedb/affixes/`（affix_by_slot.json 等三件+112 pages）与 `data/poedb/unique_jewels/`（两 JSON+cache），报告 `docs/research/poe-affix-mapping.md` + `poe-unique-jewel-adaptation.md`，数据地图 `data/poedb/README.md`。affixes 原始缓存 117MB 已清理（重跑可重建）。**注意**：早期 data/poedb/*.json 九件为旧雏形种子，本轮真实数据为准。

- **V1.27 商店缺件根修 + 词缀计数修正 + 点金分级/新工艺 + UI 重排出包部署 PASS（真机二轮回弹四点反馈）**：用户回传 Player.log + 截图。①缺件根因实锤：日志 `WP row missing for GlobalID=91003/91004`——`PoeItemMod.BuildAccessoryRow` 给戒指/项链设 `PLtype=4`（全职业），而 `AddRow` 首行守卫 `mb.PLtype < im.Weapon.GP.Length`（GP 长度=4）**恒假**，模板行从未进入 Legendary 池 → 上架时 `FindRow` 落空静默跳过。修复=删 PLtype 守卫（按 CharType 投递全部 4 个职业池）+ 模板 PLtype 改 0；V1.26 查重/补架/日志保留。②词缀计数：用户实测原生传说盾显示 11/9 应为 9——元素行（冰霜/闪电/物理穿透=一次元素词缀拆分）改为计 1 条（`HasAnyElement` 布尔），基础三围/武器技能/凹槽不计。③点金分级泛化 `ExecAlchemy(w,targetQuality,opName)`：点金石→稀有、点金石·精致→精致、点金石·史诗→史诗、传说石→传说、神话石→神话（均普通品质起，4~该档上限条词缀，受禁骰限制，失败回退品质；传说/神话石后续拟改物品兑换）。④UI 重写 V1.27：标题28pt/金币19pt/目标19pt 分区排布修遮挡、「货币工艺」「工艺限制」分区行、行配色分层+行名加粗、列表下移。构建 0 error(124 存量) → 部署 SHA256 `B82B5E91…1C9`（2,439,680 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `ShadowDungeon-MOD-V1.27_2026-08-29.zip`（SHA256 `8CCA2A7A…E51`）。CHANGELOG V1.27 + status + todo + code-index 已更新。**待真机**：商店 5 件齐全、词缀计数、点金分级/新石、UI 观感。

- **V1.26 工艺台适配 + 商店 5 件自愈出包部署 PASS（真机首轮回弹三点反馈）**：用户真机验证 V1.25 进关卡不崩后反馈：①商店 5 件只出 3 件（疾风/洞悉之瓶+万箭之玉在，星环之戒/回响之链缺——恰为两条武器路径上架件）②工艺台字体小、选装备要点击不要悬停③POE 工艺按游戏原生品质档适配词缀数。执行：①商店自愈=上架挪 `CreatShop` 尾部（原版随机货源先铺）+每件 GlobalID 查重+`ShopManager.SortBuy` 尾部挂钩 `PoeItemMod.VerifyShopStock` 校验补架+逐步日志（`[PoeItemMod] staged equip 91003 size=1x1 at x,y` 等）；静态深挖（vanilla CreatWeapon 与注入路径逐行同构、SlotData.ItemType setter EnsureItemData、ResolvePreparedBuySlot 兜底、AddWeaponToSortBucket 1×1→AxA 桶、AocaoPrice 字典）无法复现缺件→以自愈兜底+日志定位残余根因。②`AffixCap` 阶梯=普通0/魔法4/稀有6/精致7/史诗8/传说9/神话10（对齐原生掉落：固定词条+1随机+技能词条 Q0-3:0~1/Q4:1~2/Q5:1~2/Q6:2+SPC+元素行）。③工艺台=标题26/行19/目标金币18/图例16、行高40、EventSystem Raycast 点击选装（排除面板自身防误选）、目标行"词缀 n/cap"。构建 0 error(124 存量) → 部署 SHA256 `69CAE7CB…1C8`（2,438,656 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `ShadowDungeon-MOD-V1.26_2026-08-29.zip`（SHA256 `A77DF435…ED6F`）。CHANGELOG V1.26 + status + todo 已更新。**待真机**：商店 5 件齐否（缺则回传 Player.log 的 `[PoeItemMod]` 行）+ 工艺台点击选装/字体/上限显示。

- **V1.25 终版重建（全字段审计后三处根因全修，取代早版 B36E8004）**：早版 V1.25（B36E8004，仅修 PlayerManager 一处）出包后，本会话用原版 DLL ilspycmd 反编译树做全字段审计（先剥方法体再比对、含 `= new …()` 初始化器字段），又掘出两处漏网：① ACT_skillSample.SkillName（技能标签系统 V1.12 新增，经 ACT_skillData 内联嵌在崩溃报文点名的 ACTListSkillBT 中）② V1.24 WeaponClass.Craft_* 4 字段（被 CharButton/Hand/ItemScript/DropItemController 内联序列化）——三处（含早版已修的 PlayerManager.BS_ExtraProjectiles）全部补 [System.NonSerialized]，Unity 序列化面与原版完全一致；其余差异均为 ILSpy 渲染噪音（异步状态机字段/Spine 命名空间别名/private struct SortEntry/事件/static）。重建 0 error → 部署 SHA `F35A0E1C…61F4` 一致 → VM 42s 冒烟 LOG CLEAN → V1.25 包重制（zip `5A2A3652…5B21`）。早版 B36E8004 与 V1.23/V1.24 包均停用。

- **V1.25 热修复出包部署 PASS（真机进关卡崩溃：PlayerManager typetree 偏移；并入 V1.24 铁匠工艺台）**：用户真机（G:\SteamLibrary，RTX 5070）回传 Player.log——`A scripted object (probably ACTListSkillBT?) has a different serialization layout (Read 1184 but expected 1228)` → `level1 is corrupted! [Position out of bounds!]` → `Crash!!!`。定位：V1.23 在 `PlayerManager` 新增 `BS_ExtraProjectiles` 用了 `[HideInInspector] public`——**HideInInspector 不阻止序列化**，玩家对象在关卡场景/预制体里，运行时 typetree 比数据文件多一个字段 → level1 反序列化错位崩溃；与 V1.0 SK_FlyA 公开字段 P0 同类（铁律：Mono 新增公开字段必须 `[NonSerialized]`）。修复 1 行加 `[System.NonSerialized]`；ilspycmd 逐字段 diff 验证（V1.22 767 字段 vs 本版 768，唯一差异即该字段带 NonSerialized）→ 序列化面与原版一致。**level1 与存档未损坏（误报），SaveManager 时间戳警告为崩溃恢复噪音**。⚠️ 并行协作事件：修复重建时工作树已含并行会话 V1.24 铁匠工艺台源码（CraftBenchUI/CraftBenchOps/工艺锁），本构建（`B36E8004…5579`，2,436,608 B，元数据实测含 CraftBench*/BS_ExtraProjectiles/PoeItemMod）= **V1.24 全部功能+热修复合并版**；V1.24 包（`45DB54BE…72F7`）基于未修复 V1.23 树，**含崩溃 bug 已在 CHANGELOG 标注勿安装**。VM 42s 冒烟 LOG CLEAN。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.25_2026-08-29.zip`（SHA256 `CAB54BDF…D12F` 812,097 B）。CHANGELOG V1.25 + V1.24 警示 + status + todo 已更新。**待真机**：用 V1.25 重装→进关卡验证不崩→验收 5 件商品+工艺台。
- **V1.24 铁匠工艺台出包部署 PASS（POE metamods 工艺，13 工艺 + 4 限制，每项 1 金币）**：用户要求参考 poedb.tw/cn/metamods「工艺互动」表给铁匠增加装备各类制作选项，各选项暂定固定 1 金币，设计思路从原生游戏找。侦察：铁匠=NpcType.Rebuild→WeaponManager（三锻造面板）；品质 0-6=原生 Normal/Magic/Rare/Exquisite/Epic/Legendary/Mythical（Quality_Group）；词缀体系=Main/DOT(WPDT_A)+SK/CP(WPDT_B)+元素六行+SPC，随机池 RateMain/RateDot/RateSK/RateCP/SPC 挂在 Item_MB 模板、成长公式 GenerateWeaponStatValue/GivePRC_*；存档=WeaponSaveData 字段级拷贝、Clone=ItemCloneUtil。执行：①新文件 `PoedbMod/CraftBenchOps.cs`——13 项工艺（蜕变/增幅/改造/富豪/点金/混沌/隐匿混沌/崇高/无效/神圣/重铸/兽猎移前增后/移后增前）+ 4 项工艺限制切换，锁矩阵对齐 poedb 表（改造=前后缀锁、崇高/富豪=攻击法术禁骰、隐匿混沌无视禁骰、重铸保锁组并清全部限制、无效四锁全吃、神圣吃前后缀锁）；词条分组=前缀(主属性/持续/技能/武器元素) 后缀(同伴/抗性/SPC)、攻击(主/技/武元) 法术(持续/同伴)；词缀上限魔法 4/稀有精致 6/史诗+ 8 ②新文件 `PoedbMod/CraftBenchUI.cs`——运行时 uGUI：克隆 Close 按钮做「工艺台」开关、面板锚定 MainGroup、Scroll 17 行、目标=悬停背包武器槽缓存（同三锻造 GetMainSlot）、打开时退出锻造模态+ToggleInteract(false) 防穿透、执行链 RemoveMoney(1)→BindWeaponToRegion→ShowWPTipA→锻造音效 ③`ItemManager.cs`：WeaponDropContext/WeaponStatGroup 转 public、SetWPdata 重置工艺锁、尾部桥接 region（CraftPickPoolTemplate 按品质档选同族模板池等 11 个公共方法）④`WeaponClass.cs` 4 锁字段+InitDefault ⑤`WeaponSaveData.cs` 持久化 ⑥`ItemCloneUtil.cs` 拷贝 ⑦`WeaponManager.cs` Install 挂钩+2 访问器。构建修复 8 错（QualityColor 是 Dictionary<int,string> 非 Color[]、GameManager 在 Core 命名空间、SetElement 漏写）→ **0 error**(124 存量) → 部署 SHA256 `45DB54BE…72F7`（2,436,608 B）一致 → 冒烟进程存活 3 分钟 + Player.log 四项 0 命中 PASS。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.24_2026-08-29.zip`（SHA256 `AEBF835E…628E`）。CHANGELOG V1.24 + status + todo + code-index 已更新；设计记录 `docs/research/craftbench-metamods-design.md`。**边界**：VM 无法悬停选择背包，工艺台 UI/操作游戏内实操验收留真机。**【V1.25 后注】该包构建于未修复的 V1.23 树，含 typetree 崩溃 bug，已被 V1.25 取代。**

- **V1.23 POE 测试装备包出包部署 PASS（5 件商人 0 元商品）**：用户要求新增 5 件测试装备固定上架商人且 0 元——①功能药剂（POEDB 式可重复饮用+持续+冷却）②投射物环形发射传奇戒指③投射物最远距离/无法击中后返回项链④所有投射物 +1 镶嵌珠宝⑤第 5 件补位=第二瓶功能药剂。执行：新文件 `PoeItemMod.cs`（GlobalID 91001-91005 定义/CharBT 穿戴门控/黄金角 137.5° 旋转器/单体弹补射/Item_MB 行运行时合成（Quality5+PLtype4+DropLevelStart999 不进掉落池/图标音效借同部位原版饰品）/CreatShop 头部固定上架 1 号买页前 5 格 Price=0/Item_FY LOC 反射注入含语言切换重注）+ 手术点：药剂链（UseItemClass.Use 两 case=AddPotionBuff+AddSimpleDrink、BuffPotionItem Init/DelBuff/文案 MVSpeed_Tmp+BJrate_Tmp、BuffManager 图标、InventoryManager UseItem/UseItemACT_Use+WarehouseManager 三处 IsRepeatableFlask 不消耗门控、UseItemACT 快捷栏 case）、戒指（Gun 四出手点黄金角+dic 随角、SK_Angle_F Type0/1/2→SpawnEvenRing 全环）、项链（SK_FlyA ReturnToPlayer 追加 ReturnEquipped）、珠宝（GetSocketType "projectile"→26、ApplySocketedGemStats case26→PlayerManager.BS_ExtraProjectiles、Sanitizer GemFloatFields+26、ACTbar SetSkill_Sample 两处 Count_F+Extra 与 Gun 单体补射互斥防双倍、BaoshiClass.GetMain tooltip、ShopManager 新增 CreatBS）。构建 0 error(124 存量) → 部署 SHA256 `8661C351…EA505`（2,411,008 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.23_2026-08-29.zip`（SHA256 `E15A895B…3ECD` 800,479 B）。CHANGELOG V1.23 + status + todo + code-index 已更新。**边界**：全环化覆盖 Angle 弹幕族；返回覆盖 SK_FlyA 族；单体补射跳过 BuffTime>0 技能防 SK_BuffA 重复注册。

- **V1.22 主题迭代出包部署 PASS（火球术紫色 / 冰晶术亮红）**：用户试玩 V1.21 后指定换色。纯常量迭代验证"颜色收敛为常量"设计：①FxSpriteFactory 火焰图集配色抽为 FireTip/Body/Core/DarkColor 四常量，当前=紫色系（#D9A8FF/#A855F7/#5B1FA8/#241040，烘焙进 8 帧图集）②SK_FlyA 三色常量中性化命名 CoreTint/MainTint/DeepTint（消除 Ice 前缀与主题解耦）、方法改名 ApplyShurikenStyle，当前=亮红系（#FF5450/#FF2E29/#B80A1A）③SK_FlyBall 仅注释更新。构建 0 error(124 存量) → 部署 SHA256 `9A08B9C3…96FB`（2,397,696 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.22_2026-08-29.zip`（SHA256 `634254D1…B93B` 795,435 B）。CHANGELOG V1.22 + status + todo 已更新。

## 2026-08-28

- **V1.21 火球术 Flipbook + 冰晶术 Shuriken 特效出包部署 PASS（真机试玩版，基于 V1.20 源码）**：用户看过三方案演示页后指定——火球术改方案一（自制序列帧）、冰晶术改方案三（Shuriken 深度程序化），真机试玩。执行：①新文件 `FxSpriteFactory.cs`：纯静态运行时贴图工厂（软圆/碎片/8 帧 512×64 火焰图集全 SetPixels 代码生成，浮点缓冲 over 合成+固定种子；材质克隆游戏 par/parLoop 粒子 shader 换 _MainTex/_TintColor，回退 Sprites/Default；SpawnFlipbookBurst + SpawnIceBurst 一次性发射器；零外部资产零 POE 素材无 public 序列化字段）②火球术方案一（SK_FlyBall.cs）：SetStart 识别 FireBall 启动 Flipbook，Update 顶 14fps 循环换 Arrow.sprite（不受 CanMV 影响），TimeStop/Stop 双路径一次性爆裂护栏 ③冰晶术方案三（SK_FlyA.cs）：撤黑炎版，ApplyShurikenIceStyle=白蓝亮核+拖尾三段渐变(+0.06s max0.3)+AttachShardTrail 碎片子发射器(8/s colorOverLifetime)；StartReturn 头部 SpawnIceBurst 末段爆裂（覆盖命中/超时，护栏）+清理；Stop 兜底；回旋不变 ④构建修复 3 错（gravityModifier 属性名、ShapeModule.rotation 为 Vector3）→ 0 error(124 存量) → 部署 SHA256 `410407D5…F370`（2,397,696 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.21_2026-08-28.zip`（SHA256 `96A5B71A…82B4` 796,125 B）。CHANGELOG V1.21 + status + todo 已更新。观感迭代=改 SK_FlyBall/SK_FlyA 类头常量。游戏内观感验收待真实机器。
- **V1.20 技能参数"串台"修复 + 节点自动摆发出包部署 PASS（V1.19 两项观感修复）**：用户截图+补充线索（"新技能显示的是上一个悬停的原生技能"）反馈 V1.19 两问题。取证链：①导出新版 SampleF（sharedassets1 path_id **1272**，旧工具硬编码 1276 首次导出错误对象，AssetScan 重扫定位）比对——tooltip 蓝耗30/CD12/350%/需14点与 **Gale Arrow** 行逐项吻合；②导出新版 Skill_FY（resources path_id **472**，旧 433 已失效）——`info_Gale Arrow`="传送到目标位置并召唤6个龙卷风" 与截图描述逐字吻合、`Gale Arrow`→狂风箭（非旋风斩）；③结论：存在 IndexName="Cyclone" 但数值=Gale 的混合对象 → 读 GetManaSample 源码锁定根因：**克隆时把 SonA/SonB/SonC 清成 "0"，而 GetManaSample/GetCD_Sample/GetBuffTime 无空判解引用 Sample_S[SonA]/[SonB]（仅 SonC 有 "0" 特判）→ ManaCost_Last 抛 NRE → ShowSkilltip 在"标题已赋值、正文未赋值"之间中断 → 正文残留上个悬停技能**（与用户补充逐字吻合）。修复：①Son 链保留模板原值（Tornado←Razor 的 Longbow/MultiShot；Cyclone←PotW 的 Whirlwind/Stormblade/Tailwind）②位置改**锚定格周边空闲槽位自动搜索**（网格步长 155×170 实测自截图，候选同行右→左→下→上→斜角→外扩，与全部既有节点间距 ≥45px+面板边界内，两克隆互斥占用），废弃旧版 (95,-95) 固定偏移。构建 0 error(124 存量) → 部署 SHA256 `38327E60…F670`（2,387,968 B）一致 → 42s 冒烟 LOG CLEAN。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.20_2026-08-28.zip`（SHA256 `B2B76F79…9DDC` 792,510 B）。CHANGELOG V1.20 + status + todo + code-index 已更新。**经验**：游戏数据约定 SonA/SonB 必须为合法 Sample_S 键；菜单态冒烟无法覆盖 tooltip/加点等对局内路径。
- **三方案动态演示页交付（fx-alternatives-demo.html）**：用户要求把 Flipbook/CC0 素材包/Shuriken 程序化三种"动画感"方案做成可视演示。产出 `game-systems-ref/poe-assets/fx-alternatives-demo.html`（自包含、纯 Canvas 离线运行、~470 行）：①方案一=程序生成 8 帧 sprite sheet 图示 + 14fps 帧动画播放（命中爆炸节奏）②方案二=软圆/火花/烟/碎片四张底材贴图 + 三个持续发射器同屏演示"一底多色"（三主题各一发射器，加法混合发光+普通混合烟）③方案三=Trail 拖尾弹体→Sub-Emitter 末段爆裂（火花+闪光+碎片+重力）+右下角 Size/Color over Lifetime 曲线小图；全局支持 火球术/冰晶术/黑炎 三主题切换（配色取自观感规格卡），右上放 POEDB 真实图标对照。验证：Edge 实测渲染（窗口级截屏）——首轮全空白，加 window.onerror 错误条定位到 `BASE` 贴图创建参数传反（`texSoft(64,texSoft)`→`makeTex(64,texSoft)`）修复后三面板+主题切换全通过。已登记调研报告 §8.3。浏览器演示产物，无游戏/工程改动。
- **POE 素材抓取管线修复并执行成功 + 动画可用性定论（追补调研）**：用户要求修抓取脚本补素材，并调研"动画能否直接用、不能则给替代方式（不照抄，类似即可）"。执行：①探测推翻旧结论——CDN 对**不存在的猜测路径返回 403（非 404）**，对真实路径浏览器 UA+Referer 即 200；抓 `/cn/Microtransactions` 总页（5.6MB）提取 **1389 条真实 `*_Effect` MTX 目录** ②新工具 `game-systems-ref/poe-assets/fetch-assets.py`（替代删除的 fetch-icons.ps1）：目录缓存→词级匹配（排除 Portal/Pet/外观类）→抓各 MTX 页提取真实图标 URL+YouTube 视频 ID+中文名→下载（可重跑断点续传）③执行结果 **ok=41 / no-direct-mtx=90 / failed=0**，`skills/` 69 文件（41 图标+28 视频缩略图），manifest 回写真实 URL/status（no-direct-mtx=POE 本就无此类 MTX：buff/被动/DOT/同伴 + 少量当初错误猜测如 Razor Arrow 并非 POE 技能名）④动画定论：POE 特效本体/webp 图标/YouTube 视频**均不可直接进游戏**（版权+格式+技术三重否决，抽帧做序列帧也算衍生复制），视频只有人工观感参考价值 ⑤替代方案定稿（不照抄路线）：游戏内粒子重着色（Tier1 已验证）> 自制序列帧 Flipbook > CC0 素材包（Kenney Particle Pack/Unity Particle Pack，可合法随包分发）> Shuriken 程序化 ⑥产出 2 张示范观感规格卡（火球术↔Dragon_Fireball、冰晶术↔Swordfish_Ice_Spear，配色三件套+落地参数）。报告追补 `docs/research/poe-fx-fusion-survey.md` §8 + TL;DR 更新；poe-assets/README.md 重写；fetch-report.json 落盘。未动游戏/工程代码（注：本条目进行期间检测到并行会话已出 V1.19 注入挂钩修复，素材工作与其零冲突）。
- **V1.19 技能注入挂钩修正 + 自愈出包部署 PASS（修复 V1.18 节点不显示）**：用户真机反馈游侠天赋面板看不到 Tornado Shot/Cyclone（只有原生 3 系）。取证：`TalentManager.OpenClose()` 全工程零调用方（死方法），面板真正入口是 `GameUIManager.OpenClose_Talent()`（直设 BottomCAV[3] 可见性，不经 TalentManager）→ V1.18 按钮克隆从未执行。修复四件：①挂钩挪至 `OpenClose_Talent` 打开分支（HasInstance 护栏+try/catch）②`TryEnsureButtons` 自愈化——先 `TryInjectData` 再补按钮，打开面板一步到位 ③去跨局静态短路（`_dataInjected`/`_buttonsEnsured` 改 ContainsKey/FindButton 天然幂等，重进对局不漏注入；LOC fallback 进程内一次）④按钮搜索三级兜底（tm 层级→FindObjectsOfType→Resources 含未激活 scene.IsValid 过滤）+ Rewire 兜底（模板 text 未初始化时直找克隆体 "Text" 子节点）；TalentManager.OpenClose 死方法上的 V1.18 挂钩移除零残留。构建修复 2 错（迭代器 yield+catch 拆分、GameUIManager 无 using System 改 System.Exception）→ 0 error(124 存量 warning) → 部署 SHA256 `9AD86F9A…D4C8`（2,386,944 B）比对一致 → 冒烟 2 分钟+ 存活 + Player.log 四项 0 命中 PASS（VM 前台焦点弹回无法驱动游戏 UI，可视验收留真机）。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.19_2026-08-28.zip`（SHA256 `EB863B26…E644` 791,774 B）。CHANGELOG V1.19 + status + todo + code-index 已更新。
- **POEDB 素材×游戏特效融合全景调研落盘（纯调研，3 车道）**：用户要求多 agent 调研 POEDB 技能/装备效果哪些可完美融合、哪些需优化/特化/修改，本次只调研；如需实测须用独立文件夹。执行：①后台子 agent 3 条车道全部秒败（provider "Model request failed"/"user concurrency limit exceeded"，重试 1 条仍败）→ 按 2026-08-26 先例改编排层直接执行 ②素材盘点：`game-systems-ref/poe-assets` 实际仅 2 个示例文件（webp=静态 VP8X 图标 + YouTube 缩略图 jpg），131 条映射全部 `guess-需校验`；fetch.log 实录 **18 连败 403 Forbidden**（修正此前"404 属预期"记录），skills/ 空、fetch-report.json 未生成；补全路线=页面抓真实 URL + YouTube 缩略图批量 ③管线底座：确认可动层（Arrow/trail/par 粒子/EXP/FStype 分支）与改造分级 Tier1 换色（V1.18 ApplyBlackFlameStyle 已验证）/Tier2 运行时 PNG 贴图+参数特化/Tier3 资产级/墙（Spine/FMOD/webp 不支持）④融合矩阵：python 全量 131 条判级 **A 可完美融合 20 / B 需优化 87 / C 需特化 24 / D 0**，Top-10 A 候选（烈焰箭↔Burning Arrow、剃刀箭↔同名、火球术↔Fireball 等）+ Top-5 C 重设计（暴风雪→落点型、雷霆跃迁→MGC FStype=3 本就瞬移、箭雨→落点多弹等）⑤4 主题评估：装备/怪物词缀=纯文本参照、宝石图标可批量拉、锻造=UI 参照。报告落盘 `docs/research/poe-fx-fusion-survey.md`；status/todo 已更新。**未建测试文件夹、未动任何代码/资产**。
- **V1.18 技能注入追加式重写 + 冰晶术黑炎特效出包部署 PASS（推倒 V1.9~V1.17 两条错误路线）**：用户反馈注入全链路异常（布局乱/原技能被顶/串台/无法施放）+ 冰蓝特效观感差，要求推倒重做不得在错误版本上打补丁。确认三决策：症状全选、**追加式**、特效改**黑炎测试版**（实体不动，箭体黑炎色+粒子暗红，观感由 agent 保障）。执行：①侦察锁定根因——替换式把 Razor Arrow 原位覆写为 Tornado Shot 且把箭矢模板硬改 FStype=7（环绕分支=箭矢挂玩家转圈，即"行为不对"元凶）、SampleF 语义核验（FStype 7/8/9=环绕 SetParent、AllChuan_F 0=穿透）、环绕模板盘点（Xi 0/3/6/9 各有 Flamestrike/Holy Sword/Power of the Wind/Blood Pool）②删除 `PoedbReplaceInjector.cs` + 972 行旧克隆式 `PoedbSkillInjector.cs`，全新追加式注入器（数据：Start 首行 TryInjectData 克隆模板追加 Tornado Shot/Cyclone 至 Sample_F/SKI/FW，原技能零改动；按钮：OpenClose 打开分支 TryEnsureButtons 整格克隆+相对路径重接+原生自动注册；LOC fallback 文案与列值一致）③TalentManager 挂钩 5→2 零残留 ④SK_FlyA 撤销 V1.16/17 全部特效（IceDust/StopIceDust/OnDisable 兜底/_iceDustGO），新增 `ApplyBlackFlameStyle()`（Arrow 0.16,0.05,0.07 近黑炎核 + trail 黑红三段渐变 time+0.05max0.25 + par 原生粒子全层暗红 0.55,0.07,0.10，3 个 BlackFlame* 常量）⑤构建 0 error(124 存量 warning) → 部署 SHA256 `66CCC0A0…CBEB`（2,384,384 B）比对一致 → 冒烟 40s 存活 + Player.log 四项 0 命中 PASS（VM 点击派发受窗口身份校验限制，游戏内可视验收留真机）。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.18_2026-08-28.zip`（SHA256 `7D5578D9…EB00A` 791,577 B）。CHANGELOG V1.18 + status + todo + code-index 已更新。
- **V1.16 冰晶术秘法特效出包部署 PASS（SK_FlyA A+B2 车道）**：冰晶术 Ice Crystal（Xi1 风暴领主 OBJ6）视觉增强——`SK_FlyA.SetStart()` 尾部 `try` 内追加 `ApplyMysticIceStyle()`（保留 ReturnToPlayer 回旋），`Arrow 0.6,0.92,1` + `material.SetColor` 冰蓝、`Trail 0.5,0.85,1,0.9→0.7,0.92,1,0 + Gradient + time+0.12s(max0.35s)`、`par` 冰蓝 tint、`IceDust` 代码动态 `ParticleSystem` 15/0.4s/0.12/8/Circle 0.15r/`Particles/Additive`。仅单文件 `SK_FlyA.cs` 全路径 try/catch 零风险。构建 0 error(124 warning) → 部署 SHA256 `1CF2F53A…0FDB`（2,406,912 B）比对一致 → 冒烟 38s 存活 + Player.log 四项 0 命中 PASS。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.16_2026-08-28.zip`（SHA256 `2B93C74F…D509` 797,375 B，含 install.ps1 + README）。CHANGELOG V1.16、status 已更新。（注：本版本特效已被 V1.18 黑炎版整体替代）
- **POE 商城特效融合可行性研判 + 全主题图标批量拉取（示例端到端）**：用户要求 POE 网站下载 + 技能特效融合分析（示例 Mystic Burning Arrow）。执行：①图标批量：`MysticBurningArrowEffect.webp` 6,236 B + 视频缩略图 30,481 B 已入库 `game-systems-ref/poe-assets/`，`manifest.json` 131 技能全映射（localCN/EN↔poeCounterpart↔poePage↔poeIconGuess）+ `fetch-icons.ps1`（跳过已存在/重试3/限速）+ `fetch.log`/`skills/`。首轮 guess 404 属预期（POE 非每技能有独立 Effect 名）。其余 4 主题 icon 边界已在 manifest otherThemes 固化（装备/怪物词缀文本无图标，宝石/锻造按需拉取）。②研判：`ora-1` 审计 `Gun.CreatSP→SK_FlyA Arrow/trail/par/FX/EXP` Built-in 2D 管线，结论直拷不可行（GGG ToU 7b/7f + IP 声明 + 格式不兼容 + 资产不在 DLL 工作流）/ 参考复刻可行（A 换色 + B2 代码挂尘 80% 还原，A+B2 2天可出）；法务/素材链路 `lib-1` 补查确认 `Content.ggpk` 可提但禁商用。报告落盘 `docs/research/poe-mtx-effect-fusion-example.md`（11KB）+ `poe-assets/README.md`。

## 2026-08-27

- **游戏系统参考库建成（5 个 AGY 并行梳理）**：用户要求分别启动多个命令行，用多个 AGY 梳理装备词缀/镶嵌物词缀/怪物词缀/锻造效果/技能体系并持久化为表格+说明，独立文件夹 + AI 入口文档。执行：①并行启动 5 个 agy（gemini-3.7-flash-high + effort high）分别梳理——装备词缀（01：230+ 效果项/7 级稀有度，源 WeaponClass/ItemManager/WPDT_A/B/SaveDataEquipmentSanitizer + 5 张 CSV）、镶嵌物（02：137 项，源 BaoshiClass/WPAocao/WeaponBaoshiApplyUtil/BaoshiManager/BaoshiSettings）、怪物词缀（03：20 条 + 精英/BOSS 专属 ID15 召唤精通/ID16 多重射击 + 防御塔专属池，源 LevelManager 三 Creat*/Set* 链路）、锻造效果（04：33 项核心机制，源 WeaponManager/WeaponBaoshiApplyUtil/BaoshiManager/WeaponSettings）、技能体系（05：核心 131 项 + 267 增幅子节点 + 50 巅峰天赋，源 SkillData 七子类/PlayerActionManager/Gun/TalentManager）②5 份文档均含概述+表格（含代码位置列）+「说明」章节，落盘 `game-systems-ref/`（01~05 + README.md 入口：文件索引、给 AI 的修改指导——定位/构建 0 error/备份部署/冒烟验证/CHANGELOG 登记/描述同步红线/平衡性注意）。纯文档产出，无源码/资产改动，无需构建部署。

## 2026-08-26

- **ShadowDungeon 新版 V1.8 出包部署（背包排序栏左侧垂直重排 + 分页修复，des-1/fix-2 双车道）**：用户反馈 V1.7 两个问题——①HUD 排序按钮位置不对，要求贴着物品栏左侧边、从上到下为等级/稀有度；②背包从 2 页变成 1 页。执行：①des-1（designer）改 `InventorySortBar.cs`——按钮组从顶部水平改为贴左外侧垂直排列（anchor=(0,0.5)/pivot=(1,0.5)/x=-8px/垂直居中，组尺寸 120×68），顺序等级上/稀有度下，`SortModeBase` 同步（等级→Level 基值 2、稀有度→Quality 基值 0）②fix-2（fixer）重写 `InventoryManager.ApplySort`——从"仅当前页 + single:true 重建"改为"跨全部 MainPages 收集 + single:false 跨页重建"（复用已验证 SortAll 流程）+ 补 RebindVisibleItemObjRegions/EnsureCurrentPageItemObjs，修复排序后 2 页变 1 页（旧逻辑当前页放不下物品被 Throw 丢弃）。合并构建 0 error → 部署 SHA256 `71D874BF…66A8`（2,335,744 B）比对一致。**冒烟测试由用户人工完成**（本版本未自动冒烟）。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.8_2026-08-26.zip`（SHA256 `307081F8…48A9D8`，含 UTF-8 BOM install.ps1 + README）。CHANGELOG 登记 V1.8。
- **ShadowDungeon 新版 V1.7 出包部署 PASS（背包稀有度/等级排序）**：用户要求用 AGY（agent 系统）做 HUD 修改——背包增加装备等级/稀有度排序，并制作为更新包。确认三项决策：顶部排序按钮栏 / 全部物品 / V1.7。执行：①侦察新版背包结构（InventoryManager extends ContainerManager，基类含 cav/IVgird/slotSize/closeBtn/leftPage/pageText/MainPages/EPPages/CurPage；全部物品统一继承 ItemClass 基类 Quality/Level 字段；新版无 ApplySort 需新增）②新增 `InventorySortBar.cs`（全静态实现，P0 typetree 教训——不在现有 MonoBehaviour 上新增实例字段；稀有度/等级两按钮右对齐格子区顶部，点击重排、同键翻转升降序，激活态高亮+↑↓箭头）③`InventoryManager.ApplySort(mode)` 新增公共入口（复用 SortAutoWeapon/Baoshi/UseItem 管线；复合比较器=Quality/Level→Price 降序回退→收集序号兜底；全部物品统一按 ItemClass 基类字段排序）④`InventoryManager.Update` 首行注入 `InventorySortBar.Tick(this)`（按 cav 可见性驱动显隐）。构建 0 error（116 warning 存量）→ 部署 SHA256 `88C6AC1A…689795`（2,335,744 B）比对一致 → 冒烟 40s 存活 + Player.log 四项零命中 PASS。升级包 `MODworkv2/builds/ShadowDungeon-MOD-V1.7_2026-08-26.zip`（SHA256 `86A7D8E9…B7F8D`，含 install.ps1 一键安装 + README 更新说明）。CHANGELOG 登记 V1.7。游戏内可视验收（排序按钮栏渲染、稀有度/等级排序行为）待真实机器。
- **工作区重组 + 升级包制作**：用户要求把本次新建立内容拷贝到新文件夹 `MODworkv2`，归档旧版游戏与旧 MOD 内容，更新根目录 MD 文档，并制作一键安装升级包。执行：①`MODworkv2/` 建立（decompiled 反编译工程 + refs 引用 + backup 新版原版 DLL 备份），csproj HintPath 从 `..\refs-v2\` 改为 `..\refs\`，构建复核 0 error ②`_archive/` 建立，旧游戏 `暗影地牢 Demo` → `_archive/暗影地牢 Demo_archived`、旧 MOD 工作区 `modwork` → `_archive/modwork_archived`，附 `_archive/README.md` 归档说明 ③根目录 MD 文档更新（AGENTS.md/codemap.md/CHANGELOG.md/docs/status.md 等指向新结构）④升级包 `ShadowDungeon-MOD-V1.6_2026-08-26.zip`（含安装脚本 install.ps1 + 更新说明 README + DLL），一键安装到 ShadowDungeon。
- **ShadowDungeon 新版 V1.6 出包部署 PASS**：用户要求对 `ShadowDungeon`（新版纯净游戏）做两项试验性技能修改——①增加技能标签 ②把冰锥术（确认=冰晶术 Ice Crystal）设置成可返回。侦察发现新版 DLL（2,313,728 B）**不含任何旧 mod 符号**（ReturnToPlayer/CustomEquipGate/SkillTagSystem/ArcBoomerang 全为 0），与旧版 `暗影地牢 Demo` 为两个独立游戏。执行：①ilspycmd 8.2.0 反编译新版 DLL → `modwork/decompiled-v2`（895 .cs）+ `modwork/refs-v2`（126 引用 DLL）；csproj 引用全部指向 refs-v2；修复 2 处 `array[^1]`（System.Index 在 netstandard2.0 缺失→`array[array.Length-1]` 语义等价）；构建 0 error（116 warning 存量）②技能标签：新增 `SkillTagSystem.cs`（移植旧版，适配新版 `LOC.MM.GetSkill`），◆元素系+◇行为形态双维实时推导，注入 GameUIManager.ShowSkilltip/RefreshSkilltip 尾部，「回旋」白名单登记 Ice Crystal ③冰晶术返回：`SK_FlyA.cs` 新增 ReturnToPlayer/returning/StartReturn/StraightReturnMV，门控 `skillName=="Ice Crystal"`，命中（非穿透）与超时（TimeStop）双路径触发返回，返回途中关碰撞防二次伤害，飞回玩家 0.6f 内回收。构建 0 error → 备份新版原版 DLL（`Assembly-CSharp-v2-original.dll` SHA256 `6AD75B77…BFE01`）→ 部署 SHA256 `2AA98341…E8E1`（2,329,600 B）比对一致 → 冒烟 40s 存活 + Player.log 四项零命中 PASS。CHANGELOG 登记 V1.6（ShadowDungeon 新版独立条目）。游戏内可视验收（tooltip 标签渲染、冰晶术返回行为）待真实机器。
- **背景车道失败记录**：exp-1/exp-3（explorer 侦察）因 provider 端点不可用失败；fix-1（fixer 反编译建工程）因模型配置错误失败。均改为编排层直接执行完成，无半成品写入。

## 2026-08-25

- **V1.5 背包筛选行重做出包部署 PASS（同日第四轮）**：用户反馈页签栏遮挡格子，要求格子下移一格+整行筛选器（左种类/右稀有度·获取时间·等级排序）。执行：①exp-4 摸底（IVgird 容器与格子生成公式、Quality/Level 字段齐备但全工程无获取时间、SortCur→九桶→Price 降序现成管线、SaveData 为 plain class 加字段安全）②fix-5 车道 G（InventoryCategoryTabs.cs 单文件：EnsureGridShifted 幂等下移 Gird 一格；筛选栏 parent 改挂 IVgird 锚定顶行 +slotSize×0.5 无时序依赖；底板拉通全行宽=格子区宽−8）∥ fix-6 车道 S（新 InventorySortBar.cs 488 行：右对齐三按钮+ConditionalWeakTable 时间注册表防串档；三 SaveData 加 AcquiredAt+存读映射；InventoryManager.ApplySort(mode) 复合比较器+全入包路径打点；刷新经公共 API NotifyPageChanged 解耦）。两车道写范围零重叠并行。构建 0 error → 部署 SHA256 `52427E63…D516`（1,718,784 字节）→ 冒烟 42s 存活 LOG CLEAN → 补丁包 `ShadowDungeon-HUD-Patch-V1.5_2026-08-25.zip`（SHA256 `34C34E4A…F627C`）。CHANGELOG 登记 V1.5（期间误替换 V1.0 标题一次，已即时修复并全文结构复核）。
- **V1.4 合并终版（HUD V1.3 + 技能协同装备化）—— 终版生成与登记**：读取“Game skill tree rework with skill synergy effects”会话最新落点（CustomEquipGate.cs 新增：Ring=Custom1/Return=Custom2 装备门控；Gun.cs 4 处 `Has(RingItem)?z2+=RingCursor.Next()`；SK_FlyA.cs:535 `ReturnToPlayer=Has(ReturnItem)`；ItemManager.cs `CreatCustomEquips()` 商店注入 90001-90003），与当前 HUD V1.3（夹缝带三页签+SkillTagSystem 标签+全表文档）对比——零文件级冲突（HUD 动 InventoryCategoryTabs/SkillTagSystem，协同动 Gun/SK_FlyA/ItemManager/CustomEquipGate），源码盘已自然共存。clean 构建 0 error（114 警告存量）→ 部署 SHA256 `B5D16760…02FA` 与 V1.3 同哈希（协同改动已在 V1.3 构建时即存在）→ 42s 冒烟 LOG CLEAN。登记：CHANGELOG V1.4 合并条目 + status/worklog/todo/code-index 状态更新；补丁包 V1.3 即为合并终版物化包（V1.4 文档版同哈希复用，无需重打）。
- **V1.3 HUD 迭代出包部署 PASS（同日第二轮）**：用户反馈三项——背包页签位置丑且不通顺、技能树双页退回（保留标签）、要求列出全部标签含义。执行：①obs-1 截图分析（金币框内部仅130×50px放不下文字页签；最优=人偶区/格子区夹缝带，~900px 全宽空置）②fix-3 精确回退 TalentPageFilter（删文件+TalentManager 五处+GameUIManager 一行，grep 零残留，SkillTagSystem 无损）③des-1 designer 车道 provider 网络错误失败（核查无半成品写入）→ fix-4 按 obs-1 锁定规格机械重实现（锚(0,1)/pivot(0,1)、y=-H×0.29、x=+12px、页签100×28间距12字号17、半透明深色底板 alpha140 不挡射线）。构建 0 error → 部署 SHA256 `B5D16760…02FA`（1,709,568 字节）→ 冒烟 42s 存活日志零异常（游戏本体更新0830 新基线 vanilla 先行冒烟 PASS，再在新 refs 上 0 警告重建） → 补丁包 `ShadowDungeon-HUD-Patch-V1.3_2026-08-25.zip`（SHA256 `3CF4D7DB…6800`）。CHANGELOG 登记 V1.3。
- **技能标签全表落盘**：`docs/research/skill-tags-catalog.md`——元素系◆12 系（Xi IndexName→Skill_FY 中文名）+ 形态◇14 词（直射/位移/环绕/落点/穿透/命中爆炸/末段爆裂/多弹/追踪/减速/六种DOT词/回旋白名单），每个标签含触发字段、数值来源（_Last 实时性）、隐含机制说明；与 SkillTagSystem.cs:221-389 三个 Collect* 方法逐行对照一致。type1/3/5/6 子节点只显示元素维的设计理由一并记录。
- **V1.2 HUD 改造出包并部署 PASS**：三车道并行实现——①技能树「主技能/增幅」顶级双页（`TalentPageFilter.cs` 新增 + TalentManager 页签克隆/四处联动刷新 + GameUIManager.OpenClose_Talent 一行钩子；主技能页=type0/2/4，增幅页=type1/3/5/6；克隆体剥离持久化组件防误触，幂等 guard）；②背包「药水/镶嵌物/通用」三分类页签（`InventoryCategoryTabs.cs` 新增 + InventoryManager 两处最小挂钩；视图过滤式 alpha×0.25+禁射线，同签再点=全部，翻页/开关面板/0.5s 节流重施，WarehouseManager 零变化）；③技能 tooltip 双维标签实时同步（`SkillTagSystem.cs` 新增 + GameUIManager 三 tip 方法尾部各一行注入；元素系◆蓝 12 系 GetSkill 优先/常量回退 + 形态◇橙规则表（直射/位移/环绕/落点/穿透/命中爆炸/末段爆裂/多弹/追踪/减速/DOT 词/回旋白名单）；_Last 访问器含天赋加成→加点即刷；RegisterTagContributor 装备化二期扩展钩子预留）。构建修复 richText CS1061×2（防御块与本工程 UI 模块 API 不符，删除冗余块）。0 error 构建 → 备份核验（7A78C36C… 在位）→ 部署 SHA256 `DABDD364…0C779` 比对一致 → 冒烟 42s 存活 + Player.log 四项零命中 → 游戏进程已清理。assets 未动。（注：其中技能树双页功能已于同日 V1.3 按用户要求移除）
- **HUD 改造侦察与定案**：exp-1（docs 全量）/exp-2（HUD 代码地图）双只读车道并行回传；用户四项决策锁定——源码基线=modwork/decompiled（一期 mod 特性随包回归）、页面划分=主技能(type0) vs 子节点(type1)、背包归类=方案A（药水 InfoType0/1·镶嵌物 Baoshi·通用其余）、标签粒度=元素+形态双维。写范围三分区互不重叠（A: TalentManager/OpenClose_Talent；B: InventoryManager/ContainerManager 增量；C: 三 tip 方法体），C 串行于 A 之后复用其会话上下文。

## 2026-08-24

- **版本管理文档建立**：根目录核心文件 `CHANGELOG.md` 落盘（登记规则 + 当前部署状态 + V1.0/V1.1 条目）。版本编排按用户指定：V1.0 = fix-l1 POE 三技能运行时注入；V1.1 = 另一会话投射物机制包（SK_FlyA 回旋镖返回 + P0 [NonSerialized] 序列化修复 + StraightReturnMV、ArcBoomerang CSV 节点 + UI 克隆、剃刀箭环形 8 箭、Skill_FY 描述同步 772→774，真机验收 PASS）。AGENTS.md 同步登记：修改与验证流程新增第 5 条强制版本登记、文档维护职责表新增 CHANGELOG.md 行。当前游戏目录为原版纯净基线（DLL 7A78C36C…），V1.0/V1.1 均未部署；CHANGELOG 中已标注 V1.0 重出包必须基于含序列化修复的当前源码。
- **desc-sync 描述同步车道完成（staging，未部署）**：落实 AGENTS.md「技能修改描述同步」红线并执行存量同步。①规范：skill-spec.md infoKey 升为必填+新增「描述同步要求」节；AGENTS.md 技术红线追加描述同步条目；KI-002 待修复→处理中。②资产级：DescSync 工具（AssetsTools.NET 3.0.5 裸读/定点拼接/重开自验）对 resources.assets Skill_FY JSON（path_id=433）更新 `info_Razor Arrow`（EN/简中/繁中 → 环形8箭+回旋文案）并新增 `ArcBoomerang` 名键 + `info_ArcBoomerang` 描述键（772→774 键）；导出核验 sharedassets1 两行 Info 列已正确、无需改动。自验全 PASS：JSON 结构断言、未触碰键全量比对、PocCsvRow 独立导出回读仅 12 行预期差异、游戏目录零写入。报告与 staging：`modwork/asset-inventory/desc-sync/desc-sync-report.md`。
- **ring-arrow 部署本机 + 冒烟 PASS**：ring-test DLL（B738EB58…BCBF）覆盖部署至游戏 Managed 目录（原版备份 7A78C36C… 在位），SHA256 比对一致；启动冒烟 35s 存活 + Player.log Exception/Crash/TypeLoad/NullReference 零命中。保持部署，待真实机器验收「8 支环形箭 + 全员回旋」。
- **ring-arrow 测试包产出（未部署）**：Gun.ARCattack case 0 追加 Razor Arrow 环形发射——主箭逻辑保持不变，`dt.skillName == "Razor Arrow"` 时 for i=1..7 调 CreatSP() 复制完整弹体数据，position=ARCpointA、angle=z2+i*45°，按 RTtypeOBJ(0=rotation/1=dic) 定向 + RTtypeFX 出特效 + CreatACT_SK 武器词缀 proc；SK_FlyA.ReturnToPlayer 默认 true 使 8 支均回旋。构建 0 error（114 warning 均为存量），产物 `modwork/builds/ring-test/Assembly-CSharp.dll` SHA256=B738EB58…BCBF（1,704,448 字节）。ilspycmd 反编译自验：环形循环存在且 "Razor Arrow" 字面量全类仅 1 处，其他 case 未动。部署路径与回滚见本次任务报告。
- **V3 序列化修复真实机器验收 PASS，ArcBoomerang 端到端闭环**：用户实测可游玩电脑（RTX 5070）——V3-fixed DLL 进入 Play 正常，天赋树弓系第一页 ArcBoomerang 节点可见、0 门槛加点、快捷栏施放、箭矢回旋行为确认。根因修复有效：SK_FlyA.ReturnToPlayer 加 [System.NonSerialized] 恢复 typetree 布局（字段插在类中部导致后续序列化字段偏移→原生读取越界）。观感问题返回点 45° 偏差记 KI-001（低优先）。隔离测试全记录：原版 DLL 全正常/PoC assets 正常/新 DLL 必崩 → V1/V2 均崩排除注入器与克隆 → PRISTINE 对照锁定序列化字段。
- **「自然语言→添加技能→添加效果」框架启动**：架构定为 NL→会话内解析→spec.json→SkillForge 工具（确定性执行）→staging→部署→真实机器验收。文档三件套落盘：`docs/skill-spec.md`（v1 架构+工作流+语义规则）、`docs/effects-library.md`（Tier1 CSV 列覆盖全自动 / Tier2 代码模式库）、`docs/known-issues.md`（KI-001~004）。fix-17 实现 SkillForge 工具中（run/verify 双模式+自验+报告）。
- **崩溃二分诊断变体包产出**：真实机器测试定位崩溃元凶在重编译 DLL 内（原版 DLL 正常、assets 两版均正常）。步骤 0 基准构建哈希 = FFD42295…C225 与部署版一致（源码无漂移）；产出三个变体至 `modwork/builds/`：V1-no-injector（PoeSkillInjector.Bootstrap 首行早退，4E51E3F1…58FA）、V2-no-injector-no-clone（再加 TryCloneArcBoomerangButton 首行早退，9A21C72A…4ADF）、FULL-restored（还原后重建 = FFD42295…C225，与基准逐一相等证明还原无损）。测试决策树与回滚说明见 `modwork/builds/BISECT-README.md`；源码已还原为完整功能版。
- **poc-arcboomerang 端到端 PoC PASS（真实部署，保持中）**：弓 ARC 系天赋树（Xi=6）新增技能节点 ArcBoomerang。①分析：Skill_FY（resources.assets path_id=433）按 弓/箭/弩 关键词命中 43 键→回 SampleF 定位弓系行 Xi=6/7/8（代码侧 SetStart PLType==2 启用 XiBT[6..8] 交叉印证），选 Razor Arrow（Xi6/OBJ38，第 40 行）为模板行。②资产侧：新建 `modwork/tools/PocCsvRow/`（export/addrow 双模式，裸读 m_Script+ContentReplacerFromBuffer 写入），克隆模板行仅改 IndexName/name=ArcBoomerang、Info=info_ArcBoomerang、Price=0、UnLock_Point=0，追加至末内容行后（游戏解析器 Split('\n') 尾部空项前），自验 PASS（72 行×152 列、原 71 行逐列无差异）。③代码侧：TalentManager.cs 单文件改动——Start() 首行注入 TryCloneArcBoomerangButton()：校验 Sample_F.ContainsKey 后 FindObjectsOfType 找 Xi=6/SkillType=0 模板按钮→克隆整格（含 Text 子节点）→相对路径重接 text/SkillTU 引用→anchoredPosition+(95,-95)→改 IndexName/Xi/SkillType；不手动 AddListener/Register（原生 OnEnable/Start 自动走 RegisterSkillBT+SetSkillBT，避免监听器双挂）；全程 try/catch 防御。④构建 0 error；部署前核验备份哈希（CA85E2A4…FEDB1 一致）；双文件部署 SHA256 比对一致（DLL FFD42295…C225 / assets 4B4E7C10…50B2）。⑤启动验证：40s 存活 + Player.log Exception/Crash/TypeLoad/NullReference 零命中 → PASS 保持部署。报告（含回滚两条命令与手工验收步骤）：`modwork/asset-inventory/POC-REPORT.md`。注：本 VM HomeScene 预存崩溃未复现阻碍本次菜单态验证；节点显隐/施放验收待可用环境。
- **fix-l1 attempt 002**：三路线取证。A（batchmode 绕渲染）：`-batchmode -nographics` 下游戏在引擎初始化期即原生崩溃（早于 Player.log 创建，同 0x80000003），headless 不可用；常规模式 + autostart 复跑 4 次全部在 HomeScene 加载同一位置崩溃——插桩显示进程私有内存冲至 5.2GB、系统提交余量最低 3.1GB（非耗尽），判定为确定性原生读取失败而非资源竞争。B（side-car）：注入器新增 `SideLog()`（`poemod-injection.log` 追加写，try/catch 包裹），已随最新构建部署，供可用环境取证。C（逻辑级最强证据）：`modwork/tools/InjectHarness/` 无头 harness 扩展为全管线验证——真实 CSV donor → 真实 CloneSample/Build* → 真实 SkillXiData 字典注册 → SetSkill_Sample 数据包复刻断言 → 合成 fire DOT + SetDot 复刻断言 → 快捷栏绑定状态断言，**27/27 通过**；引擎边界（SingletonMonoScope.Instance 原生 FindObjectOfType / LeanPool UI 槽）如实标注。最终部署菜单态复验：42s 存活零异常（游戏本体更新0830 新基线 vanilla 先行冒烟 PASS，再在新 refs 上 0 警告重建）。
- **fix-l1 POE 投射物技能注入器实现并部署**：新增 `modwork/decompiled/PoeSkillInjector.cs`（静态类 + RuntimeInitializeOnLoadMethod/BootstrapEntry 双入口，每秒轮询注入）+ `BootstrapEntry.CreateBootstrap` 一行挂钩。三个技能：POE_Fireball（克隆 FireBall/Xi0，colEXP→0 命中即爆 + 合成 fire DOT 25%/4s）、POE_IceSpear（克隆 Ice Crystal/Xi1，AllChuan_F→0 全穿 + MoveSpeedCut 30/DebuffTime 2 减速 + 双弹）、POE_LightningArrow（克隆 Lightning Ball/Xi2，直飞全穿多段 + LastEXP→0 末段 AoE）。构建 0 error，SHA256 校验部署。**数据逻辑经无头 harness 全部断言通过（33/33）**。
- **关键资产事实核验（AssetsTools.NET 裸解析）**：SKprefab(SO) 位于 sharedassets1.assets path_id=121575；SK_OBJ[70] 数组逐项解析出预制体名与组件——OBJ0="01 1 Fire Ball"/OBJ6="Angle (F)"/OBJ12="03 1 TD Ball" 均挂 SkillOBJ_DT_SP+父驱动脚本；SK_XJ_pen/SK_Pen 链仅被 FireXJ/FireTrap 关卡陷阱引用（XJ_Pen_1/2），无玩家技能可克隆该链路。SampleF CSV 实测 71 行×152 列，Xi 表 12 系 damageType 映射确认。
- **环境阻塞发现（预存问题，非本 mod 引入）**：本 VM（Hyper-V/WARP 软渲染/8GB）加载 HomeScene 时 UnityPlayer 原生层读 sharedassets1.assets 报 "corrupted/Position out of bounds" 并自崩（0x80000003）。已排除：文件损坏（与备份 SHA256 一致、135,173 对象全量 typetree 解析零失败、对象偏移全部在界）、非 ASCII 路径（C:\SDGame junction 复现）、时序竞争（4s/14s 延迟均复现）、并发实例、注入代码（不含任何注入逻辑的最小 BootstrapEntry+StartGame 构建同样崩溃）。游戏场景在本工作区从未成功加载过（Saves 目录此前为空）。真实机器上可用 `-poemod-autostart` 参数跑全自动注册验证。

## 2026-08-23

- **fix-12 测试准备完成**：UABEA v8 就位；AssetScan 扫描器构建 0 错误，7/7 容器扫描成功；219 个 TextAsset 清单落盘；技能 CSV 八表 path_id 定位（1276/1226/1266/1188/1224/1348/1204/1210，与 exp-2 字节偏移交叉吻合）；发现 Xi/Baoshi/UseItem 表中文在资产源头已损坏（导入期有损转换，Skill_FY 可恢复）。产出：`modwork/asset-inventory/REPORT.md`。
- **fix-13 试改演练六步全 PASS，资产修改管线打通**：备份（SHA256 CA85E2A4…FEDB1）→ AssetEdit 构建零错误 → SampleF 第 2 数据行 ManaCost_Base 2→20（列 20；m_Script 29,287→29,288 字节；自验 71 行×152 列除目标单元格全等）→ 部署后 35s 存活 + Player.log 零异常（游戏本体更新0830 新基线 vanilla 先行冒烟 PASS，再在新 refs 上 0 警告重建）（TalentManager.Awake 全量解析通过）→ 还原后哈希与原版完全一致。报告：`modwork/asset-inventory/DRILL-REPORT.md`。AssetsTools.NET 3.0.5 写入真实 API 已记录（ContentReplacerFromBuffer + file.Write(writer,0)）。
- **可行性报告定稿**：`docs/research/feasibility-skill-expansion.md`。结论：新增技能节点可行（A1=UABEA 改 sharedassets1 内 CSV 加行，复用索引零美术）；视听扩展 B1-B4 分级；两堵墙 = Spine 新动画（授权墙）、FMOD bank 结构修改。测试计划四步（试改演练→CSV PoC→UI 按钮 PoC）。
- **exp-2 侦察完成并合并**：8 个技能 CSV 实证内嵌于 sharedassets1.assets（字节偏移已定位）；解析器不支持引号转义、逐列 Parse；SKPB 为序列化只读数组；icon 复用零成本；UseAni 玩家侧硬编码 0-4；存档按 IndexName 键控兼容新键；SkillBT 需 Clone 方案。事实已并入 code-index/resource-index。
- **工作规范建立**：编写 AGENTS.md 工作规范章节；建立 `docs/` 文档体系（status/worklog/todo/tools-index/code-index/resource-index/research）。
- **lib-1 调研完成并落盘**：Unity 资产修改工具链报告 → `docs/research/asset-toolchain-lib1.md`。核心结论：Mono+传统布局是理想 mod 靶子；新增技能节点走 A1(UABEA 改 CSV)+A2(BepInEx)；视听走 B1(纹理替换)+B2(绕 FMOD 播外部音频)；Spine 新动画与 bank 结构修改为两堵墙。
- **测试准备车道启动（fix-12）**：安装 UABEA + 编写 AssetsTools.NET 扫描器，扫描 resources.assets/sharedassets0-2/level0-2 产出 TextAsset 清单，定位技能 CSV 候选。
- **代码地图全部完成**：5 批次 fixer（战斗核心/实体AI/UI输入/关卡数据/第三方库）62 个 codemap.md + root-scripts.md(42KB) 全部落盘核验；编排层写入 `modwork/decompiled/codemap.md` 树总览与根 `codemap.md` Atlas；AGENTS.md 注册入口。
- **Loop attempt 1 PASS**：SK_FlyA 回旋镖返回效果实现→0 error 构建→SHA256 校验部署→35s 存活+日志零异常（游戏本体更新0830 新基线 vanilla 先行冒烟 PASS，再在新 refs 上 0 警告重建）。判定文件 `.opencode/loop-history/loop-mt5x07oy-5oozp9/history-001.md`。原版 DLL 备份于 `modwork/backup/`。
- **环境基建**：winget 安装 .NET SDK 8.0.424、.NET Runtime 6.0.36、ilspycmd 8.2.0.7535（最新版有兼容坑，锁版本）；反编译 Assembly-CSharp.dll → modwork/decompiled（840 .cs）；修复 csproj 中文路径乱码（引用改指 modwork/refs ASCII 副本 127 DLL）；自动补引 UnityEngine/AssetBundleModule/UnityWebRequestModule 后基线编译 0 error。
- **基线测试**：未修改原版游戏启动正常（30s 存活），Player.log 无异常特征留存作对照。

> 基线：游戏本体更新0830（92E0120F）新 Managed 基线 0 警告重建
